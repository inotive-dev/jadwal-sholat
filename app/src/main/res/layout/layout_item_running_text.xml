<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="65dp"
        android:orientation="horizontal"
        android:paddingStart="40dp"
        android:paddingEnd="40dp"
        android:gravity="center_vertical"
        android:background="@drawable/bg_round_alpha"
        android:layout_margin="8dp">

        <FrameLayout
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:layout_marginEnd="16dp">

            <TextView
                android:id="@+id/tv_name"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_gravity="start|center_vertical"
                android:textAppearance="@style/TextAppearance.MdcTypographyStyles.Body2"
                android:textColor="@android:color/white"
                android:alpha="0.6"
                tools:visibility="gone"
                tools:text="Lorem ipsum dolor sit amet, consectetur adipiscing elit. Leo, ornare tristique iaculis tempus l"/>

            <EditText
                android:id="@+id/edit_text"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@null"
                android:textAppearance="@style/TextAppearance.MdcTypographyStyles.Body2"
                android:textColor="@android:color/white"
                android:visibility="gone"
                android:imeOptions="actionDone"
                tools:text="Lorem ipsum dolor sit amet, consectetur adipiscing elit. Leo, ornare tristique iaculis tempus "/>
        </FrameLayout>

        <ImageButton
            android:id="@+id/img_delete"
            android:layout_width="20dp"
            android:layout_height="22dp"
            android:layout_marginEnd="8dp"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_trash"
            android:background="@null"
            tools:ignore="ContentDescription" />

        <ImageButton
            android:id="@+id/img_edit"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_marginEnd="8dp"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_edit"
            android:background="@null"
            android:nextFocusRight="@id/btn_save"
            tools:ignore="ContentDescription" />

        <ImageButton
            android:id="@+id/img_speed"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_marginEnd="8dp"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_timer"
            android:background="@null"
            android:nextFocusRight="@id/btn_save"
            tools:ignore="ContentDescription" />

    </LinearLayout>

</layout>